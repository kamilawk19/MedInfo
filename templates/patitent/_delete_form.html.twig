<form method="post" action="{{ path('app_patitent_delete', {'id': patitent.id}) }}" onsubmit="return confirm('Czy na pewno chcesz usunąć wpis?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ patitent.id) }}">
    <button class="btn btn-danger">Usuń</button>
</form>
