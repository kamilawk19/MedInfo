{{ form_start(form) }}
    {# {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Dodaj') }}</button> #}

    <div class="container">
    <div class="row inline-date-div">
        <div class="col">
            {{form_row(form.Last_Supply_Date)}}
        </div>
    </div>
    <div class="row inline-date-div">
        <div class="col">
            {{form_row(form.ID_Controller)}}
        </div>
    </div>
    <div class="row justify-content-center">
        <div class="col-7">
            <button class="btn btn-primary">{{ button_label|default('Dodaj') }}</button>
        </div>
    </div>
</div>
<br>
{{ form_end(form) }}
